import { useMemo, useState, type FormEvent } from 'react'
import { Megaphone, Plus, ShieldCheck } from 'lucide-react'
import { PageHeader } from '@/components/shared/PageHeader'
import { Button } from '@/components/ui/Button'
import { InputField, TextareaField } from '@/components/ui/Field'
import { Drawer } from '@/components/ui/Overlay'
import { Badge, Panel } from '@/components/ui/Panel'
import { useAuth } from '@/features/auth/AuthProvider'
import { useRows } from '@/hooks/useRows'
import { insertRow, shortDate, updateRow } from '@/lib/data'
import { supabase } from '@/lib/supabase'

type Consent={id:string;person_id:string;state:string;created_at:string}
type Person={id:string;first_name:string;last_name:string|null;email:string|null}
type Suppression={id:string;email:string;reason:string}
type Campaign={id:string;name:string;subject:string;body_text:string;status:string;scheduled_at:string|null;created_at:string}
export function MarketingPage(){const{user}=useAuth();const{data:consents}=useRows<Consent>('marketing_consents','created_at',false);const{data:people}=useRows<Person>('people');const{data:suppressions}=useRows<Suppression>('suppressions','created_at',false);const{data:campaigns,refresh}=useRows<Campaign>('campaigns');const[open,setOpen]=useState(false);const[form,setForm]=useState({name:'',subject:'',body_text:'',scheduled_at:''});const latest=useMemo(()=>{const map=new Map<string,Consent>();for(const item of consents)if(!map.has(item.person_id))map.set(item.person_id,item);return map},[consents]);const audience=people.filter(person=>person.email&&latest.get(person.id)?.state==='subscribed'&&!suppressions.some(item=>item.email===person.email?.toLowerCase()));const save=async(e:FormEvent)=>{e.preventDefault();await insertRow('campaigns',{owner_id:user!.id,...form,scheduled_at:form.scheduled_at?new Date(form.scheduled_at).toISOString():null,status:'draft'});setOpen(false);setForm({name:'',subject:'',body_text:'',scheduled_at:''});await refresh()};const queue=async(campaign:Campaign)=>{if(!confirm(`Queue this campaign for ${audience.length} consented contacts?`))return;for(const person of audience){await supabase.from('campaign_recipients').upsert({owner_id:user!.id,campaign_id:campaign.id,person_id:person.id,email:person.email,status:'queued'},{onConflict:'campaign_id,person_id'});await supabase.from('email_outbox').upsert({owner_id:user!.id,kind:'campaign',recipient_email:person.email,subject:campaign.subject,body_text:`${campaign.body_text}\n\nTo stop promotional emails, reply with “unsubscribe”.`,send_after:campaign.scheduled_at??new Date().toISOString(),idempotency_key:`campaign-${campaign.id}-${person.id}`},{onConflict:'idempotency_key'})}await updateRow('campaigns',campaign.id,{status:campaign.scheduled_at?'scheduled':'sending'});await refresh()};return <main className="page"><PageHeader eyebrow="Consent-first outreach" title="Marketing" description="Only contacts with recorded consent and no suppression can receive a campaign." actions={<Button onClick={()=>setOpen(true)}><Plus size={16}/>New campaign</Button>}/><div className="grid cards"><Panel><div className="metric"><strong>{audience.length}</strong><span>Eligible contacts</span></div></Panel><Panel><div className="metric"><strong>{suppressions.length}</strong><span>Suppressed contacts</span></div></Panel><Panel><div className="metric"><strong>200</strong><span>Daily marketing allowance</span></div></Panel><Panel><div className="metric"><strong>100</strong><span>Reserved service emails</span></div></Panel></div><div className="grid two" style={{marginTop:16}}><Panel title="Campaigns">{campaigns.length?campaigns.map(item=><div className="attention-row" key={item.id}><Megaphone size={18}/><div className="grow"><strong>{item.name}</strong><small>{item.subject} · {item.status} · {shortDate(item.scheduled_at??item.created_at)}</small></div><Badge tone={item.status==='sent'?'success':''}>{item.status}</Badge>{item.status==='draft'&&<Button variant="secondary" disabled={!audience.length} onClick={()=>void queue(item)}>Queue</Button>}</div>):<div className="empty"><Megaphone size={36}/><p>No campaigns yet.</p></div>}</Panel><Panel title="Eligible audience">{audience.map(person=><div className="attention-row" key={person.id}><ShieldCheck size={17}/><div><strong>{person.first_name} {person.last_name}</strong><small>{person.email}</small></div></div>)}{!audience.length&&<div className="empty"><p>No subscribed contacts are currently eligible.</p></div>}</Panel></div><Drawer title="New marketing campaign" open={open} onClose={()=>setOpen(false)}><form className="stack" onSubmit={save}><InputField label="Internal name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><InputField label="Email subject" value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})} required/><TextareaField label="Message" value={form.body_text} onChange={e=>setForm({...form,body_text:e.target.value})} required/><InputField label="Optional scheduled time" type="datetime-local" value={form.scheduled_at} onChange={e=>setForm({...form,scheduled_at:e.target.value})}/><Button>Save draft</Button></form></Drawer></main>}
