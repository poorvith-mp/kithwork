import { useEffect, useState, type FormEvent } from 'react'
import { KeyRound, ShieldCheck } from 'lucide-react'
import { PageHeader } from '@/components/shared/PageHeader'
import { Button } from '@/components/ui/Button'
import { InputField } from '@/components/ui/Field'
import { Badge, Panel } from '@/components/ui/Panel'
import { useAuth } from '@/features/auth/AuthProvider'
import { useRows } from '@/hooks/useRows'
import { supabase } from '@/lib/supabase'

type Settings={owner_id:string;display_name:string;timezone:string;working_day_start:string;working_day_end:string;lunch_start:string;lunch_end:string;daily_conversation_limit:number;conversation_minutes:number;resend_daily_limit:number;transactional_reserve:number}
type Audit={id:number;action:string;entity_type:string;created_at:string;metadata:Record<string,unknown>}
export function SettingsPage(){const{factors,refreshSecurityState}=useAuth();const{data,refresh}=useRows<Settings>('owner_settings','created_at',false);const{data:audit}=useRows<Audit>('audit_events','created_at',false);const[form,setForm]=useState<Settings|null>(null);useEffect(()=>{if(data[0]&&!form)setForm(data[0])},[data,form]);const save=async(e:FormEvent)=>{e.preventDefault();if(!form)return;const{error}=await supabase.from('owner_settings').update({display_name:form.display_name,working_day_start:form.working_day_start,working_day_end:form.working_day_end,lunch_start:form.lunch_start,lunch_end:form.lunch_end,daily_conversation_limit:form.daily_conversation_limit,conversation_minutes:form.conversation_minutes,resend_daily_limit:form.resend_daily_limit,transactional_reserve:form.transactional_reserve}).eq('owner_id',form.owner_id);if(error)alert(error.message);else await refresh()};const removeFactor=async(id:string)=>{if(factors.length<=2)return alert('Keep both enrolled authenticators. Add a replacement before removing one.');if(!confirm('Remove this authenticator?'))return;const{error}=await supabase.auth.mfa.unenroll({factorId:id});if(error)alert(error.message);else await refreshSecurityState()};return <main className="page"><PageHeader eyebrow="Private workspace" title="Settings" description="Owner identity, exact operating limits, authentication, and audit history."/><div className="grid two"><Panel title="Operating rules">{form&&<form className="stack" onSubmit={save}><InputField label="Display name" value={form.display_name} onChange={e=>setForm({...form,display_name:e.target.value})}/><div className="grid two"><InputField label="Work starts" type="time" value={form.working_day_start.slice(0,5)} onChange={e=>setForm({...form,working_day_start:e.target.value})}/><InputField label="Work ends" type="time" value={form.working_day_end.slice(0,5)} onChange={e=>setForm({...form,working_day_end:e.target.value})}/></div><div className="grid two"><InputField label="Lunch starts" type="time" value={form.lunch_start.slice(0,5)} onChange={e=>setForm({...form,lunch_start:e.target.value})}/><InputField label="Lunch ends" type="time" value={form.lunch_end.slice(0,5)} onChange={e=>setForm({...form,lunch_end:e.target.value})}/></div><div className="grid two"><InputField label="Daily conversations" type="number" min="1" max="12" value={form.daily_conversation_limit} onChange={e=>setForm({...form,daily_conversation_limit:Number(e.target.value)})}/><InputField label="Minutes per slot" type="number" min="15" max="120" step="15" value={form.conversation_minutes} onChange={e=>setForm({...form,conversation_minutes:Number(e.target.value)})}/></div><Button>Save operating rules</Button></form>}</Panel><Panel title="Authenticator security"><div className="notice-box"><ShieldCheck size={17}/> Every data request also requires an AAL2 authenticator session.</div>{factors.map((factor,index)=><div className="attention-row" key={factor.id}><KeyRound size={18}/><div className="grow"><strong>Authenticator {index+1}</strong><small>{factor.status} · added {factor.created_at?new Date(factor.created_at).toLocaleDateString('en-IN'):'recently'}</small></div><Badge tone="success">TOTP</Badge><Button variant="ghost" onClick={()=>void removeFactor(factor.id)}>Remove</Button></div>)}</Panel></div><Panel title="Recent audit events" className="settings-audit">{audit.slice(0,30).map(item=><div className="attention-row" key={item.id}><div className="grow"><strong>{item.action}</strong><small>{item.entity_type} · {new Date(item.created_at).toLocaleString('en-IN')}</small></div></div>)}{!audit.length&&<p className="muted">Audit events will appear as protected actions occur.</p>}</Panel></main>}
